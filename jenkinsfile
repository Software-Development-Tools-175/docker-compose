pipeline {
    agent any


    stages {


        stage('Triggering slave job') {
            steps {
                echo 'Triggering ...'
                build job: 'slave'
            }
        }
    }
}
